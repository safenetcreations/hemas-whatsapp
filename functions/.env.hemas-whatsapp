# Governed cloud demo backend configuration for the hemas-whatsapp project.
# Same synthetic demo semantics as the local emulators; no secrets here.
# External messaging, diagnosis and real patient data remain hard-off.
HEMAS_RUNTIME_MODE=demo
HEMAS_DEFAULT_TENANT_ID=workspace_safenet_demo
HEMAS_PROVIDER_MODE=synthetic
HEMAS_INTEGRATION_MODE=synthetic
HEMAS_AUDIT_SINK_MODE=durable
HEMAS_OUTBOUND_ENABLED=false
HEMAS_APPROVAL_GATE_REQUIRED=true
HEMAS_DIAGNOSIS_ENABLED=false
HEMAS_CLOUD_DEMO_ENABLED=true

# --- Meta WhatsApp CANARY lane (connectivity proof only) ---
# Off until the dedicated canary Meta app exists. Secrets
# (HEMAS_META_ACCESS_TOKEN / _APP_SECRET / _VERIFY_TOKEN) live in
# Google Secret Manager, never in this file.
HEMAS_META_CANARY_ENABLED=true
HEMAS_META_PHONE_NUMBER_ID=1177237035483419
HEMAS_META_WABA_ID=873460998962893
HEMAS_META_ALLOWLISTED_RECIPIENTS=+94705667755
HEMAS_META_GRAPH_VERSION=v23.0

HEMAS_META_AUTO_REPLY_ENABLED=true
HEMAS_META_BOT_ENABLED=true
